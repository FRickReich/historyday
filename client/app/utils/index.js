import {
    currentDate,
    formatDate,
    addZeroToNumber,
    maxDaysOfMonth
} from './date';

import {
    getFromStorage,
    setInStorage
} from './storage';

export default { currentDate, maxDaysOfMonth, getFromStorage, setInStorage, addZeroToNumber, formatDate }
export { currentDate, maxDaysOfMonth, getFromStorage, setInStorage, addZeroToNumber, formatDate }